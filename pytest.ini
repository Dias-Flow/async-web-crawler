# async_crawler/pytest.ini
# Tells pytest-asyncio to treat every async def test_* as an asyncio coroutine
# without requiring the @pytest.mark.asyncio decorator on every single test.
[pytest]
asyncio_mode = auto
